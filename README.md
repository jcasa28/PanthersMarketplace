In order to run the code you need to add .env or App.env with the credentials in the discord on back end

Structure should be as follows:

SUPABASE_URL=<url starting with https>
SUPABASE_ANON_KEY=
SUPABASE_PROJECT_REF=